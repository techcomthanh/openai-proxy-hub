# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/openai_proxy_hub

# Session Security
SESSION_SECRET=your-secure-random-session-secret-key-here

# Application Configuration
NODE_ENV=development
PORT=5000

# Optional: Database Connection Pool Settings
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=openai_proxy_hub

# Security Notes:
# - Generate a strong SESSION_SECRET for production (32+ random characters)
# - Use environment-specific database URLs
# - Never commit the actual .env file to version control
# - Copy this file to .env and fill in your actual values